const { getUserBusinessSettingById, getUserBusinessSettingByToken, getUserBusinessSetting, addUserBusinessSetting, updateUserBusinessSetting } = require("../Models/UserBusinessSettings")

 

const errorResponse = (message) => {
    return ({
        error: true,
        message: message,
        data: null
    })
}

const successResponse = (message, data) => {
    return ({
        error: false,
        message: message,
        data: data
    })
}

exports.getUserBusinessSetting = async (req, res) => {
    let data = [];
    try {
        if (req?.body?.id) {
            data = await getUserBusinessSettingById(req.body.id);
            if (data?.length >= 1) {
                res.json(successResponse("data successfully get", data[0]))
            } else {
                res.json(errorResponse("id is invailid "))
            }
        }
        else if (req?.body?.token) {
            data = await getUserBusinessSettingByToken(req.body.token);
            if (data?.length >= 1) {
                res.json(successResponse("data successfully get", data[0]))
            } else {
                res.json(errorResponse("token is invailid"))
            }
        } else {
            data = await getUserBusinessSetting(req?.body);
            if (data?.length >= 1) {
                res.json(successResponse("data successfully get", data))
            } else {
                res.json(errorResponse("data is not availble "))
            }
        } 

    } catch (error) {
        res.json(errorResponse(error))
    }
}


exports.addUserBusinessSetting = async (req, res) => {
    let data = [];
    try {
        data = await addUserBusinessSetting(req.body);
        const user = await getUserBusinessSettingById(data.insertId);

        res.json(successResponse("data successfully inserted", user[0]))

    } catch (error) {
        res.json(errorResponse(error))

    }
}

exports.updateUserBusinessSetting = async (req, res) => {
    let keyName = ""
    let keyValue = null
    if (req?.body?.id) {
        keyName = "id"
        keyValue = req.body.id
    }
    if (req?.body?.token) {
        keyName = "token"
        keyValue = req.body.token
    }
    let data = [];
    let user = [];
    try {
        data = await updateUserBusinessSetting(req.body, keyName, keyValue);
        if (req?.body?.id) {
            user = await getUserBusinessSettingById(req.body.id);
        }
        if (req?.body?.token) {
            user = await getUserBusinessSettingByToken(req.body.token);
        }  
        res.json(successResponse("data successfully updated", user[0]))
    } catch (error) {
        res.json(errorResponse(error))
    }
}